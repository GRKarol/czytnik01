node_modules/
dist/
dist-capacitor/
.vite/
.cache/
.DS_Store
*.log
.env
.env.local
.env.*.local

# Local dev artifacts (screenshot tool, generated previews)
screenshots/
shoot.mjs

# Build artifacts that should NOT be committed.
# Drop a real rsvp-nano.bin / czytnik01.bin into public/firmware/ locally
# or have CI bake it in — never commit firmware binaries to git.
public/firmware/*.bin

# Firmware build outputs (PlatformIO)
firmware/.pio/
firmware/.pioenvs/
firmware/.piolibdeps/
firmware/.vscode/
firmware/build/

# Plugin build outputs (PlatformIO) — one stray file
# (plugins/focus-timer/.pio/build/project.checksum) was accidentally
# committed before; untracked below, ignored going forward.
plugins/*/.pio/

# Android release signing — sekrety, nigdy do repo.
android/keystore.properties
*.keystore
*.jks
