node_modules/
dist/
.vite/
.cache/
.DS_Store
*.log
.env
.env.local
.env.*.local

# Local dev artifacts (screenshot tool, generated previews)
screenshots/
shoot.mjs

# Build artifacts that should NOT be committed.
# Drop a real rsvp-nano.bin / czytnik01.bin into public/firmware/ locally
# or have CI bake it in — never commit firmware binaries to git.
public/firmware/*.bin

# Firmware build outputs (PlatformIO)
firmware/.pio/
firmware/.pioenvs/
firmware/.piolibdeps/
firmware/.vscode/
firmware/build/
firmware/pwr_debug_log.txt
firmware/pwr_debug_watch.ps1

# Capacitor web build output (mirrors dist/)
dist-capacitor/

# Built .apk drops — too large for git, distribute separately
Mobilna aplikacja/

# Telegram bot delivery folder (ephemeral)
pliki_bota/

# APK signing secrets — never commit
android/keystore.properties
*.keystore
